% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/prepare-data.R
\name{prepare_ecodata}
\alias{prepare_ecodata}
\title{Validate and prepare ecological modelling data}
\usage{
prepare_ecodata(
  data,
  config,
  predictors,
  period,
  group,
  period_levels = NULL,
  standardize = TRUE,
  complete_cases = TRUE
)
}
\arguments{
\item{data}{A data frame containing responses, predictors and grouping data.}

\item{config}{Response configuration with `response` and `family` columns.}

\item{predictors}{Environmental predictor names.}

\item{period}{Temporal-factor column.}

\item{group}{Random-intercept grouping column.}

\item{period_levels}{Optional ordered levels for the temporal factor.}

\item{standardize}{Logical; standardize numeric predictors using z-scores.}

\item{complete_cases}{Logical; use one shared complete-case dataset for all
candidate models. This should normally remain `TRUE` for valid AICc
comparisons.}
}
\value{
Prepared data with preparation metadata stored as attributes.
}
\description{
Validate and prepare ecological modelling data
}
\examples{
dat <- data.frame(
  response = c(1.1, 1.4, 1.2, 1.8, 1.5, 2),
  forest = c(10, 20, 30, 40, 50, 60),
  period = rep(c("early", "late"), 3),
  site = rep(c("a", "b", "c"), each = 2)
)
config <- data.frame(response = "response", family = "gaussian")
prepared <- prepare_ecodata(
  dat, config, predictors = "forest",
  period = "period", group = "site"
)
head(prepared)
}
